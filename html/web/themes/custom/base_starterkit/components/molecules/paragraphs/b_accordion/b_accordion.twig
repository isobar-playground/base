<div id="{{ 'b-accordion'|clean_unique_id }}" class="js-b-accordion main-container" data-accordion="collapse">
  {% for item in items | children(true) %}
    <div class="b-accordion-item">
      {{ item.content }}
    </div>
  {% endfor %}
</div>